# In production, leave these empty or remove them.
# The frontend will use relative "/api" which proxies to the same-origin backend.
VITE_API_URL=http://localhost:5001/api
VITE_SOCKET_URL=http://localhost:5001
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
